import styled from "styled-components";

const Preview = () => {
  return <PreviewWrapper>INSERT PREVIEW HERE</PreviewWrapper>;
};

export default Preview;

const PreviewWrapper = styled.div`
display: flex;
align-items: center;
justify-content: center;
position: absolute;
top: 50%;
left: 3.5rem;
font-size:4rem;
width:100%;

`;
